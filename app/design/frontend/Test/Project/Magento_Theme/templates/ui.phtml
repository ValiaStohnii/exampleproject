<div id="custom">
    <span data-bind="text: text">text</span>
</div>
<script>
    require(['knockout'], ko => {
        const viewModel = function () {
            this.text = 'text'
        };

        ko.applyBindings(new viewModel(), custom)
    });
</script>
